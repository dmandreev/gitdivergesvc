// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { BatchDivergenceData, BatchDivergenceErrors, BatchDivergenceResponse, BatchDivergenceResponses, CloneRepoData, CloneRepoErrors, CloneRepoResponse, CloneRepoResponses, DeleteRepoData, DeleteRepoErrors, DeleteRepoResponses, FetchRepoData, FetchRepoErrors, FetchRepoResponse, FetchRepoResponses, GetDivergenceCommitsData, GetDivergenceCommitsErrors, GetDivergenceCommitsResponses, HealthData, HealthResponses, ListBranchesData, ListBranchesErrors, ListBranchesResponses, ListReposData, ListReposErrors, ListReposResponses, RepoDivergenceData, RepoDivergenceErrors, RepoDivergenceResponse, RepoDivergenceResponses, TestTokenData, TestTokenErrors, TestTokenResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Run batch branch-divergence analysis across multiple repositories.
 *
 * Executes the full three-phase pipeline (clone → fetch/checkout → analytics)
 * in parallel for every repository in the request body.
 *
 * This endpoint returns a Server-Sent Events stream. Progress events are
 * emitted while git operations run in a blocking thread pool. The stream ends
 * with either a `complete` event containing the [`Vec<BatchDivergenceResponseItem>`]
 * or an `error` event.
 */
export const batchDivergence = <ThrowOnError extends boolean = false>(options: Options<BatchDivergenceData, ThrowOnError, BatchDivergenceResponse>) => (options.client ?? client).sse.post<BatchDivergenceResponses, BatchDivergenceErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/batch/divergence',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List indexed repositories.
 *
 * Reads the local repository index (`repos.json`) and returns
 * every tracked repository with its GUID, name, URL, and absolute filesystem path.
 */
export const listRepos = <ThrowOnError extends boolean = false>(options?: Options<ListReposData, ThrowOnError>) => (options?.client ?? client).get<ListReposResponses, ListReposErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/repos',
    ...options
});

/**
 * Clone a remote repository.
 *
 * Clones `url` into a GUID-derived directory. The repository is registered in
 * the local index so that subsequent calls can refer to it by GUID. If the
 * repository already exists locally, it is not re-cloned and the response
 * status is `already_exists`.
 *
 * This endpoint returns a Server-Sent Events stream. Progress events are
 * emitted while git operations run in a blocking thread pool. The stream ends
 * with either a `complete` event containing the [`CloneResponse`] or an
 * `error` event.
 */
export const cloneRepo = <ThrowOnError extends boolean = false>(options: Options<CloneRepoData, ThrowOnError, CloneRepoResponse>) => (options.client ?? client).sse.post<CloneRepoResponses, CloneRepoErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/repos/clone',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a repository from the local index and remove its on-disk clone.
 *
 * Looks up the repository by `repo_guid`, acquires the per-repo lock to
 * prevent races with concurrent fetch or divergence operations, removes the
 * local clone directory (if it exists), drops the entry from the index, and
 * invalidates any cached divergence data for the repository.
 */
export const deleteRepo = <ThrowOnError extends boolean = false>(options: Options<DeleteRepoData, ThrowOnError>) => (options.client ?? client).delete<DeleteRepoResponses, DeleteRepoErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/repos/{repo_guid}',
    ...options
});

/**
 * List branches for an existing repository.
 *
 * Looks up the repository by `repo_guid` and returns all remote-tracking
 * branch names (with the `origin/` prefix removed). The gix library provider
 * is used for the enumeration.
 */
export const listBranches = <ThrowOnError extends boolean = false>(options: Options<ListBranchesData, ThrowOnError>) => (options.client ?? client).get<ListBranchesResponses, ListBranchesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/repos/{repo_guid}/branches',
    ...options
});

/**
 * Analyse branch divergence for a single repository.
 *
 * Looks up the repository by `repo_guid`, validates the requested branches
 * against local remote-tracking refs, then computes pairwise commit divergence
 * using the `gix` library provider.
 *
 * This endpoint is **read-only** — it does not fetch from the remote or
 * mutate the working directory. Call `POST /api/v1/repos/{repo_guid}/fetch`
 * first if you need fresh remote data.
 *
 * This endpoint returns a Server-Sent Events stream. Progress events are
 * emitted while git operations run in a blocking thread pool. The stream ends
 * with either a `complete` event containing the [`DivergenceResponse`] or an
 * `error` event.
 */
export const repoDivergence = <ThrowOnError extends boolean = false>(options: Options<RepoDivergenceData, ThrowOnError, RepoDivergenceResponse>) => (options.client ?? client).sse.get<RepoDivergenceResponses, RepoDivergenceErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/repos/{repo_guid}/divergence',
    ...options
});

/**
 * Retrieve paginated missing commits for a specific branch pair.
 *
 * Requires that divergence analysis has already been run for this repository
 * (via `GET /api/v1/repos/{repo_guid}/divergence`) so the results are cached.
 */
export const getDivergenceCommits = <ThrowOnError extends boolean = false>(options: Options<GetDivergenceCommitsData, ThrowOnError>) => (options.client ?? client).get<GetDivergenceCommitsResponses, GetDivergenceCommitsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/repos/{repo_guid}/divergence/commits',
    ...options
});

/**
 * Fetch updates for an existing repository.
 *
 * Looks up the repository by `repo_guid` in the local index, runs `git fetch`,
 * validates every branch in the request body against `origin/<branch>`, and
 * checks out the valid branches locally.
 *
 * This endpoint returns a Server-Sent Events stream. Progress events are
 * emitted while git operations run in a blocking thread pool. The stream ends
 * with either a `complete` event containing the [`FetchResponse`] or an
 * `error` event.
 */
export const fetchRepo = <ThrowOnError extends boolean = false>(options: Options<FetchRepoData, ThrowOnError, FetchRepoResponse>) => (options.client ?? client).sse.post<FetchRepoResponses, FetchRepoErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/repos/{repo_guid}/fetch',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Generate a test JWT signed with the embedded local RSA key.
 *
 * This endpoint is only available when the service is configured in `Local`
 * auth mode. The returned token expires on **2035-01-01** and can be used to
 * authorise requests via the Swagger UI *Authorize* button or via the
 * `Authorization: Bearer <token>` header.
 */
export const testToken = <ThrowOnError extends boolean = false>(options?: Options<TestTokenData, ThrowOnError>) => (options?.client ?? client).get<TestTokenResponses, TestTokenErrors, ThrowOnError>({ url: '/auth/test-token', ...options });

/**
 * Health check.
 *
 * Returns a simple `ok` status and the daemon version. Use this endpoint to
 * verify that the service is running and reachable before issuing other requests.
 */
export const health = <ThrowOnError extends boolean = false>(options?: Options<HealthData, ThrowOnError>) => (options?.client ?? client).get<HealthResponses, unknown, ThrowOnError>({ url: '/health', ...options });
